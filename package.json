{
  "name": "skin-tone-variator",
  "version": "1.0.0",
  "description": "Application de variation de teint de peau avec IA",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm install --omit=dev",
    "build:frontend": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start",
    "docker:build": "docker build -t skin-tone-variator-frontend ./frontend && docker build -t skin-tone-variator-backend ./backend",
    "docker:run": "docker run -p 3001:3001 -e GEMINI_API_KEY=$GEMINI_API_KEY skin-tone-variator-backend",
    "docker:run:frontend": "docker run -p 4000:80 skin-tone-variator-frontend",
    "audit": "npm audit --omit=dev",
    "audit:fix": "npm audit fix --omit=dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
